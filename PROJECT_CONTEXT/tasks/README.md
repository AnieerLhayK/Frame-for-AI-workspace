# Task Context Domain

`tasks/` owns task routing, structured outcome records, and the human-readable
ledger. Read `registry/index.yaml` for routing and expand only the referenced
domain file. `records/` is machine-written fact storage; `ledger/` is its
human-readable continuity projection.

The old root YAML registry and old task directories are compatibility aliases;
new code and new documentation use this domain.
