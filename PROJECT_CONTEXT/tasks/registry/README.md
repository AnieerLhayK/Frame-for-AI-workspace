# Task Registry

`index.yaml` contains schema, default rules, and task ID-to-domain mappings.
Task bodies live in `domains/*.yaml`. The loader presents the canonical
single-registry shape to callers. Root compatibility projections were formally
retired on 2026-07-19.
