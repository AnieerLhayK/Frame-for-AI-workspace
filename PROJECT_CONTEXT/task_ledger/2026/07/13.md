# Task Ledger

### TASK-20260713-002 - Document ignore rule ownership

- Date: 2026-07-13
- Status: implemented
- Task type: ignore_policy_update
- Branch: main
- Commit: pending
- Read:
  - AGENTS.md
  - CLAUDE.md
  - .gitignore
  - .claude/.gitignore
  - mcp/.gitignore
  - reports/.gitignore
  - PROJECT_CONTEXT/task_registry.yaml
- Modified:
  - AGENTS.md
  - PROJECT_CONTEXT/task_registry.yaml
  - PROJECT_CONTEXT/task_ledger.md
- Decision:
  - Make directory ownership the default rule for future ignore additions.
  - Keep cross-tree patterns at the root and document the exact ownership of
    Claude, MCP, reports, and standalone skill/package rules.
  - Let CLAUDE.md inherit this policy through its existing `@AGENTS.md` entry.
- Validation:
  - `git check-ignore -v --no-index` probes confirmed the existing child rules
    still distinguish ignored runtime paths from tracked exceptions.
  - Routed task scope and workflow checks passed.
  - `git diff --check` passed.
- Knowledge writeback:
  - Written to `AGENTS.md`, the always-on owning rule; no separate
    WORKSPACE_ENGINEERING entry was needed.
- Next:
  - Apply the narrowest-owner rule whenever a new generated or local artifact
    appears.





### TASK-20260713-001 - Split directory-owned ignore rules

- Date: 2026-07-13
- Status: implemented
- Task type: ignore_policy_update
- Branch: main
- Commit: pending
- Read:
  - AGENTS.md
  - .gitignore
  - PROJECT_CONTEXT/task_registry.yaml
  - workspace_manifest.yaml
  - shared/manifest_portability_policy.md
  - shared/workspace_path_policy.md
  - existing nested `.gitignore` files
- Modified:
  - .gitignore
  - .claude/.gitignore
  - mcp/.gitignore
  - reports/.gitignore
  - PROJECT_CONTEXT/task_registry.yaml
  - PROJECT_CONTEXT/task_ledger.md
- Decision:
  - Move only rules with a clear independent `.claude`, `mcp`, or `reports`
    boundary into the matching child `.gitignore`.
  - Keep cross-directory caches, temporary files, private drops, packaging
    outputs, and shared report patterns in the root `.gitignore`.
  - Use directory-anchored child patterns to preserve the original matching
    semantics.
- Validation:
  - Ignore probes confirmed all migrated paths remain ignored.
  - Tracked candidates `.claude/settings.json`, `mcp/README.md`,
    `mcp/configs/*.json`, and ordinary reports remain visible.
  - `git diff --check` passed.
- Knowledge writeback:
  - Recorded here because this is a reusable cleanup and ownership decision;
    no new WORKSPACE_ENGINEERING method was needed.
- Next:
  - Keep `.opencode/.gitignore` as a separate follow-up because it currently
    ignores itself and is not a tracked shared rule.





### TASK-20260713-EXTERNAL-SKILL-SLICE - Deploy selected Matt Pocock skills

- Date: 2026-07-13
- Status: done
- Task type: skill_architecture_update / platform_exposure
- Branch: main
- Source snapshot: `${WORKSPACE_ROOT}/research/skills/mattpocock-skills` @ `391a270`
- Modified:
  - `workspace_manifest.yaml`
  - `external-skills/productivity/{grill-me,grilling,handoff}`
  - `external-skills/engineering/{diagnosing-bugs,tdd,code-review,codebase-design,writing-great-skills}`
  - `PROJECT_CONTEXT/todo/external-skills.md`
  - `PROJECT_CONTEXT/current_status.md`
  - `PROJECT_CONTEXT/task_registry.yaml`
- Decision:
  - Keep the deployed candidates as curated workspace sources, not raw research copies.
  - Expose all eight candidates to Codex and Claude Code through manifest-driven Junctions.
  - Constrain the adapted candidates to text-only guidance; defer `teach` because it assumes a separate stateful teaching project.
  - Keep `writing-great-skills` alongside `skill-creator`: the former is a design reference, while the latter is the creation workflow.
- Validation:
  - All eight skill lifecycle validations passed.
  - `scripts/check_links.ps1` passed for 31 projections.
  - `python scripts/workspace_cli.py health --with-tests` passed.
  - 287 workspace tests passed.
  - `python scripts/check_doc_pairs.py --validate-coverage --strict` passed.
