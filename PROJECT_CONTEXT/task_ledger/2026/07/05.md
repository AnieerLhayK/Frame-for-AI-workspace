# Task Ledger

### TASK-20260705-002 - Use local override for Claude model advice toggle

- Date: 2026-07-05
- Status: implemented
- Task type: claude_project_boundary
- Branch: main
- Commit: pending
- Modified:
  - .claude/model-routing-advice.json
  - .claude/hooks/model_routing_guard.ps1
  - CLAUDE.md
  - shared/claude/policies/model-routing-policy.md
  - USAGE_GUIDES/QUICK_START/claude_code.md
  - scripts/claude_model_advice.py
  - scripts/tests/test_claude_model_advice.py
  - scripts/workspace_health.py
  - scripts/tests/test_workspace_health.py
  - PROJECT_CONTEXT/task_ledger.md
- Decision:
  - Keep `.claude/model-routing-advice.json` tracked as the repository default.
  - Use ignored `.claude/model-routing-advice.local.json` as the machine-local
    override for day-to-day `workspace claude model-advice on|off`.
  - Make the hook and health check prefer the local override when present.
  - Keep the current machine state OFF through the ignored local override while
    preserving the tracked default as ON.
- Validation:
  - `python -m unittest scripts.tests.test_claude_model_advice scripts.tests.test_workspace_health`
    passed with 41 tests.
  - `workspace claude model-advice status` reports effective OFF through the
    ignored local override, with tracked default still ON.
  - `git diff --check` passed.
  - `python scripts/workspace_cli.py changes verify claude_project_boundary --agent codex --strict`
    and `python scripts/workspace_cli.py workflow check claude_project_boundary --agent codex`
    correctly allowed the model-advice files but returned ERROR because
    unrelated `scripts/raw_material_filter/` changes already exist outside this
    task scope.
- Knowledge writeback:
  - Not needed; this refines the existing Claude model advice control surface.
- Next:
  - Commit and push only the tracked model-advice changes. Do not commit the
    ignored local override file or unrelated `scripts/raw_material_filter/`
    changes.





### TASK-20260705-001 - Clarify Claude model advice off state and active Pro continuation

- Date: 2026-07-05
- Status: implemented
- Task type: claude_project_boundary
- Branch: main
- Commit: pending
- Modified:
  - .claude/hooks/model_routing_guard.ps1
  - CLAUDE.md
  - shared/claude/policies/model-routing-policy.md
  - scripts/claude_model_advice.py
  - scripts/tests/test_claude_model_advice.py
  - scripts/workspace_health.py
  - scripts/tests/test_workspace_health.py
  - PROJECT_CONTEXT/task_registry.yaml
  - PROJECT_CONTEXT/task_ledger.md
- Decision:
  - Split model advice CLI status into installation readiness and runtime
    enforcement so `off` clearly reports that the hook will not inject, pause,
    or block.
  - Add `Recommend Pro active` for Pro-class tasks when the current Claude
    session is already using Pro; the hook allows tools after that assessment
    instead of requiring a model-switch pause.
  - Make model-routing hook tests use explicit temporary toggle state so local
    `workspace claude model-advice off` does not make the regression suite
    depend on user preference.
  - Add the model-advice CLI helper and test to `claude_project_boundary`
    write scope because it is now part of the Claude boundary control surface.
- Validation:
  - `python -m unittest scripts.tests.test_claude_model_advice scripts.tests.test_workspace_health`
    passed.
  - `workspace claude model-advice status` now reports `Runtime enforcement:
    disabled (will not inject or block)` when the toggle is off.
  - `python scripts/workspace_cli.py changes verify claude_project_boundary --agent codex --strict`
    returned only the known declarative-scope warning.
  - `python scripts/workspace_cli.py workflow check claude_project_boundary --agent codex`
    returned only the same known warning.
  - `workspace health` reported Claude model-routing as disabled by toggle and
    non-authorizing; reports freshness still needs attention.
  - `python -m unittest discover -s scripts/tests -p "test_*.py"` had one
    unrelated failure: `AGENTS.md` startup budget is 931 tokens versus the 900
    token limit.
  - `git diff --check` passed.
- Knowledge writeback:
  - Not needed; this refines an existing Claude model advice control surface.
- Next:
  - Validate and commit only when requested.
