# Knowledge Registry

`index.yaml` contains registry rules and topic-to-file mappings. Topic bodies
live in `topics/*.yaml` and are loaded through the bounded knowledge interface.
The root `knowledge_registry.yaml` compatibility projection was retired on
2026-07-19.
