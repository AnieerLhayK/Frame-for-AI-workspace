# PROJECT_CONTEXT 报告

此目录存储面向人工的上下文摘要、会话摘要和架构说明。

它不替代工作区 `reports/` 目录。

此目录用于存放：

- 阶段摘要；
- 会话摘要；
- 架构说明；
- 长期连续性说明。

请勿将机器可读的验证输出存放在此处。此类输出应根据工作流程归属于 `reports/` 或 `packages/character-system/reports/runtime-loop/`。
