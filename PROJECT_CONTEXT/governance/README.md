# Context Governance

This domain contains the context budget and change-surface policy. Durable
workspace-wide policy remains in `shared/`; these files define PROJECT_CONTEXT
memory and routing behavior.
