# Agent Experiments

This directory accepts bounded, low-risk `record_write` output from approved
testing registrations.

Each testing agent must use its own exact subdirectory:

```text
reports/agent-experiments/<agent-id>/
```

Creating this directory does not activate an agent or grant source, structural,
platform, or snapshot-report write authority.
