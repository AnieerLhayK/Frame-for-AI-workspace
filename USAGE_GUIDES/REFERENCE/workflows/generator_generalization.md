# Workflow Reference: Generator Generalization

Generator generalization is a separate decision after a character patch.

It requires:

- linked patch;
- validation result;
- lesson;
- reason it is generalizable;
- risk if generalized;
- explicit decision.

Do not change generator assets from a single runtime failure without this review.

