# Workflow Reference: Generate Character

Use `character-generator` on a platform where it is currently exposed.

Inputs:

- authorized corpus;
- config JSON;
- desired output path;
- privacy constraints;
- target tasks.

Result:

- a generated platform-neutral character skill folder.

This workflow is appropriate for new characters, not mature manually evolved character patching.
