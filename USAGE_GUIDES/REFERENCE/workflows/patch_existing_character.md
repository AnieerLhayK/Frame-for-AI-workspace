# Workflow Reference: Patch Existing Character

Use `character-maintainer` on a declared platform exposure and enter `source_patch` only after its safeguards pass.

Inputs:

- target character folder;
- feedback, diagnosis, or handoff;
- patch constraints;
- validation criteria.

Result:

- narrow patch;
- patch note;
- validation note;
- optional generalization note.
