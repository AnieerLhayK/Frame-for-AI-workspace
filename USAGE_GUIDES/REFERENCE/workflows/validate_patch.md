# Workflow Reference: Validate Patch

Validation confirms whether a maintainer patch improved the target behavior without breaking adjacent instructions.

Check:

- changed files;
- adjacent references;
- prompt behavior;
- anti-patterns;
- rubric alignment;
- remaining issue.

Validation should produce a pass/fail decision or a clear deferred reason.

