# 模板

此目录用于存放可复用的工程模板，例如：

- 提示词模板；
- 治理模板；
- 验证器模板；
- 运行时数据包模板；
- 未来可复用的技能模式。
- 案例研究和外部知识导入结构。

本目录用于知识库模板。当前工作空间使用的运行时循环数据包模板应放在 `packages/character-system/shared/templates/` 中。

模板应鼓励注明来源、标明证据等级、支持验证、说明局限性、进行隐私审查、提供回滚机制，而非将建议呈现为普适真理。
