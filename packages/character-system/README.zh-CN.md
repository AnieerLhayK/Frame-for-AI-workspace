# 角色系统包

本包汇聚了与角色技能运行时及工程工具相关的组件。
平台名称不定义源码归属。

## 运行时

`runtime/` 包含面向用户的角色技能，用于生成任务输出。

## 工程工具

`engineering/` 包含用于构建、检查及演进运行时角色的生成、诊断与维护技能。

## 共享

`shared/` 包含仅适用于本包的协议、模板和模式。
工作区级别的路径、发现、故障处理、报告及可移植性规则仍保留在根目录 `shared/` 中。

## 报告

`reports/runtime-loop/` 存储本包持久化的诊断、交接、补丁、验证及泛化记录。
